# Git 配置说明

## 已配置的文件

### 1. `.gitignore`
- 忽略 `node_modules/` 和依赖锁文件
- 忽略环境变量文件 `.env` 等
- 忽略构建输出目录 `dist/`, `build/`
- 忽略 IDE 配置文件和临时文件
- 忽略数据库文件 `*.db`
- **保留** `backend/uploads/.gitkeep` 以便版本控制跟踪该目录

### 2. `.editorconfig`
统一编辑器配置（支持 VS Code、WebStorm、Sublime 等）：
- 使用 2 空格缩进
- UTF-8 编码
- LF 行尾符（跨平台一致性）
- 自动保存时删除尾部空格

### 3. `.gitattributes`
处理行尾符，确保 Windows/Mac/Linux 上的代码一致性：
- 文本文件统一使用 LF
- 二进制文件保持原样

### 4. `.prettierrc`
代码格式化配置（与 ESLint 配合使用）：
- 单引号
- 行宽 100 字符
- Trailing comma: es5（兼容 ES5）
- 不使用 Tab

### 5. `.eslintignore`
ESLint 检测忽略列表：
- 排除第三方库和构建输出
- 排除配置文件

### 6. `backend/uploads/.gitkeep`
空文件，确保 `uploads/` 目录被 Git 追踪

## 推荐的初始化步骤

```bash
# 1. 配置 Git 用户信息（如果未配置）
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 2. 查看待提交文件
git status

# 3. 暂存所有文件
git add .

# 4. 首次提交
git commit -m "chore: init git configuration"

# 5. 查看提交日志
git log --oneline
```

## 提交规范

使用以下格式编写提交消息：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型（type）
- `feat`: 新功能
- `fix`: bug 修复
- `docs`: 文档
- `style`: 代码风格（无逻辑改动）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试
- `chore`: 构建、依赖、工具链等

### 示例

```
feat(notes): add version history and restoration

- Save all note edits in note_versions table
- Allow users to view and restore previous versions
- Add timeline UI for version history

Closes #123
```

## IDE 集成建议

### VS Code
安装以下扩展以自动使用这些配置：
- **EditorConfig for VS Code** - 自动应用 `.editorconfig`
- **Prettier - Code formatter** - 使用 `.prettierrc` 格式化代码
- **ESLint** - 代码质量检查

### 自动格式化设置
在 VS Code `settings.json` 中配置：
```json
{
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true,
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "[typescript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```
